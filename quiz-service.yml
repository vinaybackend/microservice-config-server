spring:
  application:
    name: quiz-service

  profiles:
    active: dev

  data:
    mongodb:
      host: localhost
      port: 27017
      database: quizdb

server:
  port: 9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


