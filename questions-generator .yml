spring:
  application:
    name: questions-generator

  data:
    mongodb:
      host: localhost
      port: 27017
      database: questionsGenerator

config:
    value: "questions-generator,we are in dev"
server:
  port: 9097


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

